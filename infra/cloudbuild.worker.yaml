steps:
  - name: gcr.io/cloud-builders/docker
    args:
      [
        "build",
        "-f",
        "backend/Dockerfile.worker",
        "-t",
        "${_IMAGE}",
        "backend"
      ]
images:
  - "${_IMAGE}"
